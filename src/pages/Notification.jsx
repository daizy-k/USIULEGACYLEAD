// Placeholder page — matches US-6.1/US-6.2 in the project plan (deadline
// reminders) but isn't wired to real data yet. Replace this with a real
// query against a "notifications" collection once that's built.
export default function Notifications() {
  return (
    <div className="content">
      <div className="page">
        <h1>Notifications</h1>
        <p className="sub">Nothing here yet — this will show handover deadline reminders and updates.</p>
      </div>
    </div>
  );
}